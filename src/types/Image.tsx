/**
 * Image data.
 */
export type Image = {
    /** Pixel data of the image. */
    data: number[],
    /** Width of the image. */
    width: number,
    /** Height of the image. */
    height: number,
}
